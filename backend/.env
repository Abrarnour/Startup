# Database (local PostgreSQL — comme dans tes screenshots)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=project

# Backend
PORT=3000
BASE_URL=http://localhost:3000

# Frontend (pour le CORS)
FRONTEND_URL=http://localhost:5173

# JWT (garde ta clé actuelle si tu l'as, sinon utilise celle-ci)
JWT_SECRET=belmahi_secret_local_2025
PLATFORM_JWT_SECRET=platform_super_secret_key_2024






