# ─── Database (local PostgreSQL) ─────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=project

# ─── Backend ──────────────────────────────────────────────────
PORT=3000
BASE_URL=http://localhost:3000

# ─── Frontend (pour le CORS) ──────────────────────────────────
FRONTEND_URL=http://localhost:5173

# ─── JWT for school users (students, teachers, admin) ─────────
# BUG 6 FIX: was missing PLATFORM_JWT_SECRET — SuperAdmin login was broken
JWT_SECRET=belmahi_secret_local_2025

# ─── JWT for SuperAdmin (platform) ───────────────────────────
# BUG 6 FIX: added — was completely missing from original .env
PLATFORM_JWT_SECRET=platform_super_secret_key_2025

# ─── Public URL used by api.js on the frontend ────────────────
# BUG 6 FIX: PLATFORM_URL is now defined so api.js can reference it
VITE_API_URL=http://localhost:3000/api